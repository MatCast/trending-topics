# Linter setup
- [ ] Add `pre-commit`, `black`, `flake8` to `backend/requirements.txt` based on current state.
- [ ] Create `.pre-commit-config.yaml` at the root of the repository.
- [ ] Configure `black` and `flake8` hooks in the config file.
- [ ] Provide instructions to the user to run `pre-commit install`.
